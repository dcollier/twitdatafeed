<%= form_for(@tweetdatum) do |f| %>
  <% if @tweetdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweetdatum.errors.count, "error") %> prohibited this tweetdatum from being saved:</h2>

      <ul>
      <% @tweetdatum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
